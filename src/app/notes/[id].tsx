import { Redirect } from 'expo-router';

// Redirect directly to edit mode - no separate detail view
export default function NoteDetail() {
  // This route is no longer used - cards link directly to edit
  return null;
}

